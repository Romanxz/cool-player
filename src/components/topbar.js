import React from 'react'

const Topbar = props => {
  return (
    <>
      <div
        style={{
          position: 'relative',
          background: 'linear-gradient(to bottom, black, transparent)',
          height: '100%',
          width: '100%',
        }}
      />
    </>
  )
}

export default Topbar
